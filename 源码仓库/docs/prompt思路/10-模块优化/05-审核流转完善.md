# 审核流转完善

## 提示词

```
请完善人工审核流转功能：

1. 审核工作流完善：
   - 确认审核状态机 6 态流转：submitted → ai_reviewing → review → final_review → warehouse + rejected
   - 每次状态变更都经过 validate_transition 校验
   - 所有变更写入 audit_logs

2. 审核详情页完善：
   - 原始数据 vs 标注结果的 diff 对比视图（第 1 轮和第 2 轮）
   - AI 评分多维度可视化（柱状图/雷达图）
   - 审核意见输入框
   - 打回原因必填校验

3. 批量审核完善：
   - 批量选择后展示批量预览 Modal
   - 确认后逐条处理，显示处理进度
   - 遇到冲突（状态不允许审核）时跳过并记录

4. 审核时间线完善：
   - 从 audit_logs + results.flow_history 合并数据
   - 时间线包含：标注员提交 → AI 预审 → 复审意见 → 终审决策
   - 每条显示：角色头像、操作人、时间、动作、意见

5. Reviewer 工作台优化：
   - 按任务筛选审核队列
   - 按状态筛选（待复审/待终审/已打回/已入库）
   - 显示待审核数量角标

请逐个功能实现，每完成一个验证状态流转是否正确。

## AI 的回答

已实现：6 态流转全面校验，每次状态变更自动写入 audit_log。原始数据 vs 标注结果 diff 对比视图。AI 评分雷达图/柱状图多维度可视化。打回原因必填校验。批量审核预览 Modal + 进度条。冲突处理（跳过已锁定题目）。审核时间线从 audit_logs + flow_history 合并数据。Reviewer 工作台按任务/状态筛选。待审核数量角标。
